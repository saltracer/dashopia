<%= form_for(@dependant_task) do |f| %>
  <% if @dependant_task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dependant_task.errors.count, "error") %> prohibited this dependant_task from being saved:</h2>

      <ul>
      <% @dependant_task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :task_id %><br>
    <%= f.text_field :task_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
